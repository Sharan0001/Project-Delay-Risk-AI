[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project-delay-risk-ai"
version = "0.1.0"

[tool.setuptools]
packages = ["project_simulator", "data_pipeline", "models", "decision_support", "backend"]


